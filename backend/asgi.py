"""Точка входа для панели NetAngels.

Панель запускает объект `app` из модуля `asgi`, рабочий каталог — каталог
этого файла. Имя модуля и объекта задано пресетом Python ASGI, менять их
нельзя: они прописаны в конфигурации сайта, а не у нас.

`application` экспортируем вторым именем — так точка входа переживёт
переезд на хостера с другим соглашением.

Важное отличие от заготовки пресета: наш код нигде не опирается на
os.getcwd(). Все пути считаются от __file__, поэтому приложение не
сломается, если панель однажды запустит его из другого каталога.
"""

from server.main import app

application = app

__all__ = ["app", "application"]
