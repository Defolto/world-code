-r requirements.txt

# Тесты и линт. На сервер не уезжают: deploy ставит только requirements.txt
pytest==8.3.4
httpx==0.28.1
ruff==0.8.4

# Конвейер графики (tools/sprites.py). Живёт в этом же venv, чтобы не плодить
# окружения; на сервер тоже не уезжает
pillow==12.3.0
numpy==2.4.6

# Схема уровней и автосолвер (tools/level_schema.py, simulator/tests).
# В Pyodide не попадают: симулятор — только stdlib
pyyaml==6.0.3
pydantic==2.13.5
